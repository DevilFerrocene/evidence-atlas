---
name: evidence-atlas
description: Trace a paper’s claims and numbers through their cited sources, record what the investigation actually finds, and deliver a local bilingual annotated reader with structured evidence. Use for claim-level literature audits and their reusable website outputs.
---

# Evidence Atlas

本目录是完整、可搬运的 skill 与前后端框架。任何能读文件、查阅来源并运行本地命令的 agent 都可使用；不依赖特定模型、MCP、账户或付费 API。未支持 skill 自动发现的软件，可以直接读取本文件。运行环境和接口见 [README.md](README.md)；数据契约见 [schemas/paper.schema.json](schemas/paper.schema.json)。

## 要交付什么

让读者看原文及中英切换，悬停或点按划线观点时，先得到直接结论和判断理由，再顺着一段好读的解释理解观点怎样成立、哪里尚有疑问。数据由 JSON 驱动，具体论文和研究结论不写死在页面中。

默认详情的叙述层级是：`summary` 直接回答当前观点能说到什么程度；`why` 简洁解释这样判断的主要理由；`story` 从相关基础事实、假设或原始观测出发，用连贯自然段一步步推到当前观点。段数服从内容，不套统一模板。它不是倒序引用清单，也不是把证据卡片逐个拼成文章。重要的误引、过时、更正或保留条件，若会改变判断，就融入结论和叙述。证据图、精确定位、展开计算和补充发现放在次级展开区，完整参考文献再放低一级；默认阅读不被书目和节点标签打断。

正文、论断、证据、来源是不同实体。原文观点的身份应保留；AI 的判断、翻译和后来找到的证据要能区分。结构字段提供可追溯的连接，不替代研究判断。

## 怎样调查

根据任务和论文选择调查顺序，不规定篇数、候选数量、固定深度或“全部证实”的目标。可以先解决关键数字，也可以沿一处可疑引文追出旁支，再回来补齐全文。用户范围优先。

先取得可使用的全文，明确版本、译本、页码和纳入范围。为这篇论文保留足够的正文来支撑逐句阅读；全文展示须有公版、许可或用户提供等依据。版权不允许完整复制时，改用用户可在本地放入的原文，清楚说明当前阅读范围。摘要不能代作全文，搜索片段不能代作核读来源。

逐段审阅正文、图表、公式、脚注及补充材料中纳入范围的论断。把可单独核查的观点、数量、假设、方法选择、比较、推广和实验建议关联到稳定片段。多个观点可以共用一段，一条观点也可跨段；不为凑数量机械拆词。连接语、符号介绍和非论证文字可原样保留并写明未单列的理由。覆盖完整是阅读清点结果，不是自动提取器的自我评分。

从论文实际引用的具体段落、公式、表格和数据倒查，读到它真正依靠的理由。引用可能只是背景、转述、猜想、待验证前提、误引，或者与主张方向相反。遇到这些情况就写出来，并说明对当前观点的影响。不要把“有参考文献”当成“有证据”，也不要为了得到闭合的链而把引用者自己的复述设成证明终点。

倒查过程中遇到过时结论、新反证、撤稿、更正、版本差异、条件不符、替代解释或意外发现时，判断它是否影响正在审查的观点。相关发现放入 `findings`，连到具体来源；无需另造一套僵硬流程，也不要为了展示功能而捏造误引或过时案例。新资料的日期与原文可获得的证据要分清。

数字要找得到原始测量量、表格或公式，并说明单位、分母、样本和条件、误差含义、必要换算。可作直接算术复核；不要把重算公布数字写成重新做了实验。定理或推导可追到足够明确的假设、公式和可复核步骤。对相互矛盾的证据，分别呈现理由，允许当前判断为混合或未解决。

“追到哪里够用”由观点决定：原始测量和方法足以支持某个观测；明确的公设可以解释推导依赖，却不是对公设的实验证实；某个预言可能目前只有建议。来源拿不到、争论未读齐、证据只能支持较弱表述、需要新的测量才能前进，都可以成为有信息量的停止理由。记录已尝试且与结论相关的缺口，并说明继续调查最需要什么。不要假装查到了未取得的仪器数据，也不要求无限追到整个学科基础。

没有固定分类能概括所有调查。`assessment` 提供页面的简短状态，细节以双语 `summary`、`limits`、`findings` 和证据内容为准。`kind`、关系和停止类型允许普通字符串扩展；特殊发现可用 `findings` 表达。真正的循环引用要明确记为发现与未解决终点，别让它成为递归证明。

## 组织交付

1. 读取 schema 和 [data/papers/einstein-1905-inertia-energy.json](data/papers/einstein-1905-inertia-energy.json) 中少量相关记录，理解锚点与证据图关系。示例的物理结论不能迁移到新论文。
2. 生成新论文的 JSON：`paragraphs[].segments` 保留英中正文和稳定 ID；`claims` 整理人能读懂的结论、理由和顺向叙述。`story[].evidence_ids` 把自然段连接到依据，在默认界面不堆引用编号。`evidence` 记录带定位的理由和依赖；`sources` 记录版本、链接、查阅层级与日期。中英字段应表达相同判断，不降低原文的“不确定”“可能”语气。LaTeX 放在 `segment.latex`。
3. 证据链的叶节点需要 `terminal.reason` 说明当前停止位置。它可以是测量、推导、定义、假设、未解决问题或访问缺口。内节点也可保留局部停止说明。已读摘要、仅见书目和拿不到全文分别登记；用户提供的摘录或全文用 `provided_excerpt`、`provided_full_text`，说明实际提供范围，别把材料中的示例 URL 写成已访问。相关内容只能按实际读取层级使用。
4. 在本目录运行 `npm ci`，用 `node scripts/cli.mjs validate /absolute/path/paper.json` 验证结构。校验器检查必需字段、双语文本、引用关系、双向锚点和依赖环；它不会判定研究结论是真是假。由 agent 定向核对全文覆盖、关键数字、定位和最影响结论的支链。
5. 用 `node scripts/cli.mjs import /absolute/path/paper.json` 导入。若来源带 `local_path`，包内路径应为 `sources/<filename>`，或用 `--source-dir` 指明包含该目录的基目录。已有同名论文默认拒绝覆盖；需要更新时在用户授权范围内使用 `--replace`。数据改动后重启本项目服务。
6. `npm start` 启动本地网站。查看服务返回的 URL，确认文献与观点接口可读，再打开给用户。按当前任务做最小定向验收：双语内容、公式、划线锚点、同段多观点、来源链接、证据分支与停止说明。用户要求浏览器测试时，实际操作悬停、固定、键盘及窄屏；接口成功不能写成这些交互已实际验证。

把整个本目录交给下一位 agent，或放入其支持的 skills 目录。单独拷走本文件会失去代码、schema和例文。用 `node scripts/cli.mjs export <paper-id> <empty-output-directory>` 可单独交付一篇论文的数据及其本地来源文件。完成时提供本地网址、框架/skill入口和实际完成的例文范围；具体访问缺口只需按对结论的实际影响说明。
